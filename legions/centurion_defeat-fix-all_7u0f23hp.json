{"campaign_id": "defeat-fix-all", "tasks": [{"task_id": "fix-bridge-papyrus", "capability": "SAFE_SHELL", "prompt": "cat > /tmp/bridge_fix.py << 'PYEOF'\nimport sys\npath = '/media/paul-kane/SteamGames/Games/mods/SexLabMadness_P_v1.3.1/Scripts/Source/slmBridge.psc'\nwith open(path, 'r') as f:\n    code = f.read()\n\nold = '''    SexLab.StartScene(positions, \"Aggressive,Forced\", victim, akCenter, 1, \"SLM\")\nEndFunction'''\n\nnew = '''    ; Try progressively broader tags until SexLab finds matching animations\n    int tid = SexLab.StartScene(positions, \"Aggressive,Forced\", victim, akCenter, 1, \"SLM\")\n    If tid < 0\n        tid = SexLab.StartScene(positions, \"Aggressive\", victim, akCenter, 1, \"SLM\")\n    EndIf\n    If tid < 0\n        tid = SexLab.StartScene(positions, \"\", victim, akCenter, 1, \"SLM\")\n    EndIf\n    ; If all tags failed, clean up defeat state so victim is not stuck busy\n    If tid < 0\n        slmNative.DefeatComplete(victim)\n    EndIf\nEndFunction'''\n\ncode = code.replace(old, new)\nwith open(path, 'w') as f:\n    f.write(code)\nprint('BridgeDefeatStart patched with fallback tags + failure cleanup')\nPYEOF\npython3 /tmp/bridge_fix.py"}, {"task_id": "fix-repacify-cpp", "capability": "SAFE_SHELL", "prompt": "cat > /tmp/repacify_fix.py << 'PYEOF'\npath = '/home/paul-kane/projects/sexlab-madness-plugin/src/Plugin.cpp'\nwith open(path, 'r') as f:\n    code = f.read()\n\n# Add targeted re-pacification in the main loop where OMNI-PACIFICATION was removed\n# Find the line after the lock acquisition and delaySec calculation\nold_main = '''                if (!s.dispatchQueue.empty() && (now - s.lastDispatchTime) >= delaySec) s.ProcessDispatchQueue();'''\nnew_main = '''                // Re-pacify only actors already in defeatPacifiedMap if they re-entered combat\n                if (!s.defeatPacifiedMap.empty() && (now - s.lastPacifyTime) >= delaySec) {\n                    s.lastPacifyTime = now;\n                    auto* pl = RE::ProcessLists::GetSingleton();\n                    for (auto& [vfid, pacSet] : s.defeatPacifiedMap) {\n                        for (auto afid : pacSet) {\n                            if (auto* a = RE::TESForm::LookupByID<RE::Actor>(afid)) {\n                                if (a->Is3DLoaded() && a->IsInCombat()) {\n                                    a->StopCombat();\n                                    if (pl) pl->StopCombatAndAlarmOnActor(a, true);\n                                }\n                            }\n                        }\n                    }\n                }\n                if (!s.dispatchQueue.empty() && (now - s.lastDispatchTime) >= delaySec) s.ProcessDispatchQueue();'''\n\ncode = code.replace(old_main, new_main)\nwith open(path, 'w') as f:\n    f.write(code)\nprint('Targeted re-pacification added to main loop')\nPYEOF\npython3 /tmp/repacify_fix.py"}, {"task_id": "bump-18", "capability": "SAFE_SHELL", "prompt": "cd /home/paul-kane/projects/sexlab-madness-plugin && sed -i 's/VERSION 1.3.17 LANGUAGES/VERSION 1.3.18 LANGUAGES/' CMakeLists.txt && sed -i 's/SexLab Madness v1.3.17/SexLab Madness v1.3.18/' mod/MCM/Config/SexLabMadness/Config.json && echo 'bumped to 1.3.18'"}]}