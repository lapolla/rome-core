# Manifesto: Suture Preparation
**Role:** Imperial Archivist (ROME Legionary)
**Objective:** Backup the external MCP server and extract its final toolset.

## 1. Action: Backup
Create a compressed archive of `/home/paul-kane/projects/mcp-server/` and save it to `/home/paul-kane/tmp/mcp_server_backup.tar.gz`.

## 2. Action: Extraction
Perform a clean read of `/home/paul-kane/projects/mcp-server/server.mjs` and identify all `server.tool` definitions, including their arguments and implementations.

## 3. Output Requirements
Provide a report confirming the backup location and a complete, raw list of the tool definitions extracted.

---
**"Preserve the old to empower the new."**
