# Manifesto: Monty Python Verification
**Role:** Imperial Auditor (ROME Legionary)
**Objective:** Verify the connection and toolset of the new Python Dictator.

## 1. Action: Connection Check
Run `gemini mcp list` and confirm that `my-local-server` is reported as **Connected** using `python3 dictator/dictator.py`.

## 2. Action: Toolset Audit
Run `gemini --help` and check for the presence of the following key tools:
*   `skyrim_read_state`
*   `list_directory`
*   `shell_exec`
*   `execute_legion`

## 3. Output Requirements
Provide a report confirming the connection status and listing the tools found.

---
**"Authority is meaningless without verification."**
