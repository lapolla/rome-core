# Manifesto: MCP Rename Protocol
**Role:** Imperial Administrator (ROME Legionary)
**Objective:** Modify the internal name of the MCP server.

## 1. Target File
*   `/home/paul-kane/projects/mcp-server/server.mjs`

## 2. Modification Instruction
Locate the line `name: "ubuntu-shell-fs-server",` within the `server.mjs` file.
Change this line to `name: "asshole",`.

## 3. Output Requirements
Provide the **entire, modified content** of the `server.mjs` file. No additional text, only the code.

---
**"A server without a proper name is a server without a soul."**
